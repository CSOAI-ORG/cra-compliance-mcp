{
  "name": "cra-compliance-mcp",
  "description": "Cra Compliance Mcp tools for AI agents. Built by MEOK AI Labs.",
  "version": "1.0.0",
  "tools": [
    {"name": "check_compliance", "description": "Run a cra compliance check.", "parameters": {"type": "object", "properties": {"target": {"type": "string"}, "caller": {"type": "string"}}, "required": ["target"]}},
    {"name": "audit_report", "description": "Generate a full cra audit report.", "parameters": {"type": "object", "properties": {"target": {"type": "string"}, "caller": {"type": "string"}}, "required": ["target"]}}
  ]
}
